- name: Update Fedora server OS to the latest patches.
  dnf:  
    name: "*"
    state: latest
    update_cache: yes
  when: ansible_distribution == 'Fedora' 
  register: Fed
- name: output debug info
  debug: msg="debug info is {{ Fed }}"
